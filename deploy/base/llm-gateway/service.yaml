apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: karyaquest-services
  labels:
    app.kubernetes.io/name: llm-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-gateway
  ports:
    - name: http
      port: 8000
      targetPort: http
